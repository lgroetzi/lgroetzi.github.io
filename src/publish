#!/bin/bash
rsync -rvzc --delete \
      --exclude="/README.md" \
      --exclude="/.gitignore" \
      --exclude="/.git" \
      --exclude="/.nojekyll" \
      --exclude="/src" \
      out/ ..
git add ..
git reset -- src/
git commit -m "Update static website"
